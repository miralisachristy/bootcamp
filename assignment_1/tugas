// Variabel pertama
const name = "Lisa";

// Variabel kedua
const age = 26;

// Gabungkan variabel dalam satu teks
const message = `${name} is ${age} years old.`;

// Tampilkan pesan di console
console.log(message);